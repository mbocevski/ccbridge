#!/bin/sh
set -e
case "$1" in
    configure)
        cat <<'EOF'

  ccbridge installed. Run once per user account to wire it up:

    ccbridged setup

  This registers Claude Code hooks in ~/.claude/settings.json and
  enables the ccbridge.service systemd user unit.

EOF
        ;;
esac
exit 0
